[package]
name = "z3trace"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
simd-json = "0.14"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
